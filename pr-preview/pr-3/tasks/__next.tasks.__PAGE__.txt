1:"$Sreact.fragment"
2:"$Sreact.suspense"
3:I[42654,["/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/babff0e7418ab052.js","/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/3ecbb78a8ff87549.js","/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/c63472aac096477d.js","/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/f9f579c42c486944.js"],"TasksPageClient"]
13:I[75049,["/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/d2c13bb33c4fcd54.js","/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/c21188132e0e0d48.js"],"OutletBoundary"]
4:T1646,# Deterministic Ashley ECS Simulation on the libGDX Headless Backend

## Background
libGDX ships a non-rendering **headless backend** (`gdx-backend-headless`) intended for servers, CI runs, and unit tests. The official **Ashley** extension is a pure-Java ECS library (no native dependencies) that pairs naturally with the headless backend because it never touches `Gdx.gl*`.

You must build a small command-line program that boots an Ashley-powered libGDX game under `HeadlessApplication`, loads a scenario file describing initial entities, advances the ECS engine by the requested number of ticks with a deterministic fixed time step, and prints the final state of every entity to stdout.

## Requirements
- Implement a Gradle project that depends on:
  - `com.badlogicgames.gdx:gdx:1.13.1`
  - `com.badlogicgames.gdx:gdx-backend-headless:1.13.1`
  - `com.badlogicgames.gdx:gdx-platform:1.13.1:natives-desktop`
  - `com.badlogicgames.ashley:ashley:1.7.4`
- Implement an `ApplicationListener` that:
  - Creates an Ashley `Engine` with **at least two** `Component` types: a position component (fields `x`, `y`) and a velocity component (fields `x`, `y`).
  - Registers an `EntitySystem` that advances every entity matching `Family.all(Position, Velocity)` by `position += velocity * dt` on each engine update.
  - Loads a scenario text file (see format below), creates one Ashley `Entity` per line, and adds it to the `Engine`.
  - On each `render()` call, advances the `Engine` exactly once with a **fixed** `deltaTime` of `1.0 / 60.0` seconds (do **not** read `Gdx.graphics.getDeltaTime()` — the headless `MockGraphics` is not reliable enough for the determinism this task requires).
  - After exactly `TICKS` `render()` calls have been processed, writes the final state to stdout in the format below and exits the headless application.
- Run the program under `HeadlessApplication` (configure `updatesPerSecond = 60`) and **wait for the headless main loop thread to finish** before the JVM exits, so the stdout output is complete and reproducible.
- Provide a shell entry point at `/home/user/gdx-ecs/run.sh` that accepts the scenario file as its first positional argument and prints **only** the simulation output to stdout (Gradle progress noise, log lines, and any other stderr/stdout chatter must not appear on stdout). Suggested approach: run the Gradle build in quiet mode (`-q`) and route everything except the final program output to stderr or `/dev/null`.

## Scenario File Format
A plain UTF-8 text file. Lines starting with `#` and blank lines must be ignored. The remaining lines, in any order, are:
- Exactly **one** `TICKS <n>` line where `<n>` is a non-negative integer.
- Zero or more `ENTITY <id> <x> <y> <vx> <vy>` lines where `<id>` is `[A-Za-z0-9_]+` and the four numbers are decimal floats.

Example:
```
# bouncing demo
TICKS 60
ENTITY A 0.0 0.0 1.0 0.5
ENTITY B 10.0 -5.0 -2.0 3.0
```

## Output Format
The program must write exactly the following lines to stdout, in this order, terminated by `\n`:
```
TICK_COUNT <ticks>
ENTITY <id> x=<x> y=<y>
... one ENTITY line per entity, in the same order they appear in the scenario file ...
```
- `<ticks>` is the integer from the `TICKS` line.
- `<x>` and `<y>` are the entity's final position, formatted with **exactly three decimals** using the `Locale.ROOT` / `%.3f` convention (e.g. `2.000`, `-30.000`, `0.500`). Use `-0.000` only if the underlying value really is negative zero; otherwise emit `0.000`.
- No additional lines, no leading/trailing whitespace, no ANSI codes.

## Implementation Hints
- Use the `HeadlessApplication(ApplicationListener, HeadlessApplicationConfiguration)` constructor and set `config.updatesPerSecond = 60`. Remember the headless main loop runs on its own thread — capture that thread (e.g. via `Thread.getAllStackTraces().keySet()` filtered by name `HeadlessApplication`, or by exposing it through reflection / a custom field) and `join()` it from `main` before returning.
- Call `Gdx.app.exit()` from inside `render()` once you have processed the configured number of ticks; the headless `exit()` schedules `running = false` via a posted runnable, so cleanup happens asynchronously.
- Ashley's `Engine.update(float deltaTime)` ticks every registered `EntitySystem` in priority order. You only need one custom system for movement.
- The scenario file can live anywhere on disk; using `Gdx.files.absolute(path)` keeps things simple under headless.
- Be careful about ordering: collect entities in a list (in input order) so you can print them in the order they were declared.
- Numbers must be formatted with `Locale.ROOT` to avoid `,` decimal separators in environments with non-English locales.

## Acceptance Criteria
- Project path: `/home/user/gdx-ecs`
- Command: `bash /home/user/gdx-ecs/run.sh <scenario_path>`
- Input argument: a path to a scenario file in the format described above.
- The command must exit with status `0` on valid input.
- The stdout (excluding stderr) must contain **only** the lines described in the Output Format section, in the order specified.
- The scenario `TICKS 0` must leave every entity at its initial position (formatted to three decimals).
- A scenario with no `ENTITY` lines must still emit the `TICK_COUNT <n>` line and nothing else.
- The simulation must use the Ashley ECS extension (`com.badlogicgames.ashley:ashley`) — at least one custom `Component` class and one custom `EntitySystem` subclass must be present in the source tree under `/home/user/gdx-ecs`.
- The simulation must run under the libGDX **headless** backend (`com.badlogicgames.gdx:gdx-backend-headless`) — the LWJGL3 / desktop GL backends must not be on the runtime classpath.

5:T114a,# Deterministic Tick-Based Simulation with libGDX Headless Backend

## Background
libGDX ships a `gdx-backend-headless` module (`com.badlogicgames.gdx:gdx-backend-headless`) that boots a real `HeadlessApplication` without an OpenGL context, making it suitable for servers, simulations, and CI testing. Your task is to build a small, fully-deterministic 2D point-mass simulation that runs inside an `ApplicationListener` driven by `HeadlessApplication` and writes its final state to a file.

## Requirements
- Create a Gradle (single-module) Java project at `/home/user/gdx-sim`.
- Use libGDX `1.14.2` with the headless backend (`com.badlogicgames.gdx:gdx-backend-headless:1.14.2`) on top of `com.badlogicgames.gdx:gdx:1.14.2` and the `com.badlogicgames.gdx:gdx-platform:1.14.2:natives-desktop` natives classifier.
- Implement an `ApplicationListener` (or `ApplicationAdapter`) whose `create()` reads a properties config file via `Gdx.files.absolute(...)`, whose `render()` advances exactly one simulation tick, and whose `dispose()` writes the final state to an output file.
- Bootstrap the application from a `main` method that constructs a `HeadlessApplication` with a `HeadlessApplicationConfiguration`. After the simulation finishes, call `Gdx.app.exit()` and join the main loop thread so the JVM exits cleanly.
- The simulation must be fully deterministic and independent of wall-clock time: use the `dt` value read from the config file, not `Gdx.graphics.getDeltaTime()`.
- Make the program runnable through Gradle's `application` plugin so it can be launched with `./gradlew run --args="<config-path> <output-path>"`.

## Implementation Hints
- Look at `HeadlessApplication` and `HeadlessApplicationConfiguration` — set `updatesPerSecond = 0` so the main loop runs as fast as possible and ticks are not throttled by wall-clock pacing.
- The headless backend's `MockGraphics` cannot run OpenGL code, so do NOT call anything under `Gdx.gl*`; keep the listener pure simulation logic.
- `Gdx.app.exit()` is asynchronous (it posts a runnable that flips a `running` flag and triggers `pause()`+`dispose()`). To guarantee the output file is flushed before the JVM exits, write the file from `dispose()` and `join()` the main loop thread from `main`.
- Use `java.util.Properties` (or your preferred parser) to read the config; use `libgdx`'s `FileHandle` to read it (e.g. `Gdx.files.absolute(path).read()`).
- The integration step (Symplectic Euler — velocity is updated before position is updated using the new velocity) must be applied each tick:
  - `vx += ax * dt`
  - `vy += ay * dt`
  - `x  += vx * dt`
  - `y  += vy * dt`
  where `ax = 0` and `ay = gravity_y` are constants taken from the config.
- Count the number of times `render()` has been invoked; after exactly `ticks` ticks call `Gdx.app.exit()`. Do not perform any additional integration steps after `ticks` ticks.

## Acceptance Criteria
- Project path: /home/user/gdx-sim
- Command: `./gradlew --no-daemon run --args="<config-path> <output-path>"` from `/home/user/gdx-sim`
- The command must terminate on its own (no manual kill) with exit code 0.
- Input format (`<config-path>` is a Java properties file with these keys, all numeric except `ticks`):
  - `ticks` (integer, >= 0): number of simulation ticks to run
  - `dt` (double): fixed simulation time step in seconds
  - `position_x` (double): initial X position
  - `position_y` (double): initial Y position
  - `velocity_x` (double): initial X velocity
  - `velocity_y` (double): initial Y velocity
  - `gravity_y` (double): constant Y acceleration applied every tick
- Output format (`<output-path>` is created/overwritten by the program; UTF-8, LF line endings, one `key=value` per line, in this exact order):

  ```
  final_x=<double formatted with %.6f>
  final_y=<double formatted with %.6f>
  final_vx=<double formatted with %.6f>
  final_vy=<double formatted with %.6f>
  ticks=<integer>
  ```
  Use `Locale.ROOT` (or equivalent) when formatting numbers so the decimal separator is `.`.
- Simulation semantics: Symplectic Euler integration where every tick updates velocity first, then position. After `ticks=N` ticks, the integration step must have been applied exactly `N` times.
- `ticks=0` is a valid input and must produce the initial state unchanged in the output file.
- Output file must be flushed and the JVM must terminate within 60 seconds for any input with `ticks <= 100000`.

6:T1e74,# libGDX Headless: I18NBundle Localization Console

## Background
libGDX ships an internationalization helper, `com.badlogic.gdx.utils.I18NBundle`, that loads a family of `.properties` files (a base file plus one per locale) and resolves keys for the active locale, falling back to the base bundle when a key is missing for the requested locale. Bundles are loaded from a `FileHandle` (typically `Gdx.files.internal("i18n/Messages")`), which makes the feature easy to drive under the non-rendering **headless** backend (`com.badlogicgames.gdx:gdx-backend-headless`) because no GPU, audio, or input plumbing is involved.

In this task you will build a libGDX 1.14.2 project that boots a `HeadlessApplication`, ships a small family of localized resource bundles, and exposes a command-driven console that:

1. Switches between locales on demand.
2. Looks up plain keys with `bundle.get(...)`.
3. Formats parameterized keys with `bundle.format(...)`.
4. Demonstrates locale-to-base fallback for missing keys.
5. Reports a useful error to stderr (and exits non-zero) when the command file is malformed.

## Requirements
- Create a Gradle (Java 17) project at `/home/user/gdx-game` that depends on libGDX `1.14.2` (`gdx`, `gdx-backend-headless`, `gdx-platform:natives-desktop`).
- Implement a libGDX `ApplicationListener` (or `ApplicationAdapter`) that boots under `com.badlogic.gdx.backends.headless.HeadlessApplication`.
- Provide three properties files under the project's `assets/i18n/` directory and load them via `Gdx.files.internal("i18n/Messages")`:
  - `assets/i18n/Messages.properties` (base / English content):
    ```
    greeting=Hello
    farewell=Goodbye
    welcomeUser=Welcome, {0}!
    scoreReport=Player {0} scored {1} points
    gameName=Cosmic Quest
    ```
  - `assets/i18n/Messages_fr.properties` (French; intentionally **omits** `gameName` to exercise base-bundle fallback):
    ```
    greeting=Bonjour
    farewell=Au revoir
    welcomeUser=Bienvenue, {0} !
    scoreReport=Le joueur {0} a marque {1} points
    ```
  - `assets/i18n/Messages_de.properties` (German; also omits `gameName`):
    ```
    greeting=Hallo
    farewell=Auf Wiedersehen
    welcomeUser=Willkommen, {0}!
    scoreReport=Spieler {0} hat {1} Punkte erzielt
    ```
  Files must be UTF-8 encoded (the default for `I18NBundle.createBundle`).
- Produce a runnable launcher (via the Gradle `application` plugin) at `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game` that accepts a single `--input=<file>` argument naming a command file and drives the headless app to completion.
- Bundles must be loaded for each locale via `I18NBundle.createBundle(Gdx.files.internal("i18n/Messages"), new java.util.Locale(<code>))`. The base/default bundle (used for the `en` code and for fallback) must come from the same family.

### Command file format
- UTF-8 text file, one command per line.
- Lines starting with `#` are comments and skipped.
- Blank lines are skipped.
- Leading and trailing whitespace on each line is ignored.
- Tokens within a line are separated by one or more spaces.
- Supported commands (case-sensitive keywords):
  - `LOCALE <code>` — switch the active bundle to the locale identified by `<code>` (one of `en`, `fr`, `de`). Switching to `en` must use the base bundle family (i.e., either an explicit English locale or the base bundle directly so that the resolution behaves identically to looking up missing keys in the base file).
  - `GET <key>` — print the line `<key>=<value>` to stdout, where `<value>` is the result of `bundle.get(<key>)` on the active bundle.
  - `FORMAT <key> <arg1> [<arg2> ...]` — print the line `<key>=<value>` to stdout, where `<value>` is the result of `bundle.format(<key>, arg1, arg2, ...)` on the active bundle. Arguments are passed through as `String` values.

### Error handling
The launcher must exit with a non-zero status code and not print any further `<key>=...` lines once an error has been emitted. Errors must go to **stderr** and use the following exact prefixes:
- A `GET` or `FORMAT` issued before any `LOCALE` command:
  - `Error: no locale selected`
- An unknown locale code in a `LOCALE` command:
  - `Error: unsupported locale <code>` (use the raw `<code>` token).
- A `GET`/`FORMAT` against a key that exists in neither the active locale's file nor the base file:
  - `Error: missing key <key>` (use the raw `<key>` token).
- An unknown command keyword (anything other than `LOCALE`, `GET`, `FORMAT`, comments, blank lines):
  - `Error: unknown command <token>` (use the raw `<token>` keyword).

## Implementation Hints
- Pin `gdxVersion=1.14.2`. The Maven coordinates and Gradle wiring are summarised in the libGDX docs ([Dependency management with Gradle](https://libgdx.com/wiki/articles/dependency-management-with-gradle), [Internationalization and Localization](https://libgdx.com/wiki/internationalization-and-localization)).
- The Gradle `application` plugin gives you a turnkey launcher: configure `mainClass` and run `./gradlew installDist` to produce `build/install/<projectName>/bin/<projectName>`.
- Bootstrap the Gradle wrapper with `gradle wrapper --gradle-version 8.10 --distribution-type bin` so reproducible builds are possible from the project root.
- `Gdx.files.internal("i18n/Messages")` resolves against the `assets/` root, so put your properties files at `assets/i18n/Messages*.properties` and make sure the `assets/` directory is on the Gradle distribution's classpath (the standard pattern is to add `assets/` as a Java/resources source directory or use `sourceSets.main.resources.srcDirs += ["assets"]`).
- `HeadlessApplication` runs the listener on its own thread; block the launcher's `main` until the application has exited (for example by joining the headless main-loop thread) so the JVM stays alive long enough to flush the bundle lookups.
- Do **not** touch `Gdx.gl*`; the headless backend has no GL context.
- For locale-independent argument substitution stick to plain `{0}`, `{1}`, ... placeholders and pass the arguments as `String` values; this avoids locale-sensitive number/date formatting differences in `MessageFormat`.
- `I18NBundle.createBundle` falls back to less-specific files automatically, so a French bundle missing the `gameName` key will read it from the base `Messages.properties` without any extra code.

## Acceptance Criteria
- Project path: `/home/user/gdx-game`.
- After running `./gradlew --no-daemon --quiet installDist` from the project root, the launcher script `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game` must exist and be executable.
- Command: `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game --input=<path>`
  - `<path>` is the absolute path to a UTF-8 command file conforming to the format above.
- Successful runs must:
  - Print one `<key>=<value>` line per `GET`/`FORMAT` command, in the order the commands appear in the file.
  - Print **nothing else** to stdout (no banner, prompt, debug log, or trailing summary).
  - Exit with status `0`.
- Runs containing a malformed command must:
  - Print exactly one matching `Error: ...` line to **stderr**, using the prefixes documented above.
  - Exit with a **non-zero** status code.
  - Not emit any further `<key>=<value>` lines on stdout after the error.
- An empty command file (no non-blank, non-comment lines) must produce no stdout output and exit `0`.
- The application must boot under `com.badlogic.gdx.backends.headless.HeadlessApplication` and load each bundle via `com.badlogic.gdx.utils.I18NBundle.createBundle(FileHandle, Locale)` against `Gdx.files.internal("i18n/Messages")`. (This is verified behaviourally through the locale fallback test, which depends on `I18NBundle`'s fallback rules to resolve `gameName` from the base bundle when the active locale is `fr` or `de`.)

7:T1322,# Interpolation Curve Sampler with libGDX Headless Backend

## Background
libGDX ships a tweening utility, `com.badlogic.gdx.math.Interpolation`, that exposes named easing curves (`linear`, `smooth`, `smoother`, and many more) for animating values between two endpoints. The class is part of `com.badlogicgames.gdx:gdx` and does not require an OpenGL context, so it can be driven by the headless backend (`com.badlogicgames.gdx:gdx-backend-headless`) and used inside CI / server pipelines.

Build a small, fully-deterministic Java application that drives the libGDX `Interpolation` API from inside an `ApplicationListener` running under `HeadlessApplication`, samples a named easing curve over the unit interval, and writes the resulting samples to a file.

## Requirements
- Create a Gradle (single-module) Java project at `/home/user/gdx-interp`.
- Use libGDX `1.14.2` with the headless backend (`com.badlogicgames.gdx:gdx-backend-headless:1.14.2`) on top of `com.badlogicgames.gdx:gdx:1.14.2` and the `com.badlogicgames.gdx:gdx-platform:1.14.2:natives-desktop` natives classifier.
- Implement an `ApplicationListener` (or `ApplicationAdapter`) whose `create()` reads a Java properties config file via `Gdx.files.absolute(...)`, whose `render()` produces exactly one sample per tick, and whose `dispose()` writes the final result to an output file.
- Bootstrap the application from a `main` method that constructs a `HeadlessApplication` with a `HeadlessApplicationConfiguration`. After the last sample call `Gdx.app.exit()` and join the main-loop thread so the JVM exits cleanly.
- The sample at index `i` (zero-based) must equal `Interpolation.<curve>.apply(start, end, t)` where `t = i / (samples - 1)` as a single-precision `float` and `samples >= 2`.
- Supported curve names (must be looked up on `com.badlogic.gdx.math.Interpolation`): `linear`, `smooth`, `smoother`. The program must reject any other curve name with a non-zero exit code and an error message on stderr.
- Make the program runnable through Gradle's `application` plugin so it can be launched with `./gradlew run --args="<config-path> <output-path>"`.

## Implementation Hints
- Look at `HeadlessApplication` and `HeadlessApplicationConfiguration`. Setting `updatesPerSecond = 0` lets the main loop run as fast as possible without wall-clock pacing, which is what you want for a finite, deterministic sweep.
- `MockGraphics` in the headless backend cannot run OpenGL code, so do NOT call anything under `Gdx.gl*`; keep the listener pure logic.
- `Gdx.app.exit()` is asynchronous (it posts a runnable that flips a `running` flag and triggers `pause()` + `dispose()`). To guarantee the output file is flushed before the JVM exits, write the file from `dispose()` and `join()` the main-loop thread from `main`.
- Use `java.util.Properties` (or any other parser) to read the config; access it through `Gdx.files.absolute(path).read()` so the libGDX `FileHandle` abstraction is exercised.
- All numeric values written to the output file MUST use `Locale.ROOT` (or equivalent) when formatting, so the decimal separator is `.`.
- The sample at `i = samples - 1` must use `t = 1.0f` exactly (do not let floating-point drift produce `0.9999...`). Computing `t` as `i / (float)(samples - 1)` and then explicitly forcing the endpoint case is acceptable.

## Acceptance Criteria
- Project path: /home/user/gdx-interp
- Command: `./gradlew --no-daemon run --args="<config-path> <output-path>"` from `/home/user/gdx-interp`
- The command must terminate on its own (no manual kill) with exit code 0 on valid input.
- Input format (`<config-path>` is a Java properties file with these keys):
  - `curve` (string): one of `linear`, `smooth`, `smoother` (case-sensitive, matching the static field name on `Interpolation`).
  - `start` (double): start value of the range.
  - `end` (double): end value of the range.
  - `samples` (integer, `>= 2`): number of equally-spaced samples to produce (inclusive of both endpoints).
- Output format (`<output-path>` is created/overwritten by the program; UTF-8, LF line endings):
  - Line 1: `curve=<curve-name>`
  - Line 2: `samples=<integer>`
  - Next `samples` lines (lines 3..2+samples): one decimal number per line, formatted with `%.6f` using `Locale.ROOT`, in sample-index order (i.e. `t = 0/(samples-1)` first, then `1/(samples-1)`, ..., ending with `t = 1`).
  - The file MUST end with a trailing newline (so the total number of lines is `2 + samples`).
- Tick semantics: the listener must produce exactly one new sample per `render()` invocation. After exactly `samples` ticks, the program must call `Gdx.app.exit()`. No additional samples may be produced after `samples` ticks.
- Unknown `curve` value: the program must NOT create or overwrite `<output-path>` and must exit with a non-zero status code.
- The program must terminate within 60 seconds for any input with `samples <= 100000`.

8:T1647,# libGDX Headless Polygon Geometry CLI

## Background
You are building a deterministic command-line geometry utility on top of libGDX's headless backend. The tool reads a small text-based scripting language describing 2D convex polygons and queries, then evaluates each query using libGDX math primitives (notably `com.badlogic.gdx.math.Intersector` and `com.badlogic.gdx.math.Polygon`) inside a `HeadlessApplication` and writes one result per query to standard output. No OpenGL context, no native windowing, and no audio must be initialized.

## Requirements
- Implement a Gradle-based libGDX project at `/home/user/myproject` that runs entirely with the headless backend (`com.badlogicgames.gdx:gdx-backend-headless:1.14.2`).
- Pin the libGDX version to `1.14.2` (core `com.badlogicgames.gdx:gdx:1.14.2`).
- Wire an `ApplicationListener` (or `ApplicationAdapter`) that runs under `new HeadlessApplication(listener, config)` with `config.updatesPerSecond = 0` so the program ticks once and exits.
- Read the path to the script file from the first program argument (forwarded via `--args`).
- For each non-empty, non-comment line in the script, execute exactly one of the commands below and print exactly one output line to stdout. Use libGDX's `Intersector` and `Polygon` classes for the geometric primitives.
- After processing every line, call `Gdx.app.exit()` and join the main loop thread cleanly.
- A `run.sh` wrapper script must be provided at the project root that accepts the script-file path as the first argument and forwards it to the headless launcher, writing only the program's stdout lines (no Gradle banner / progress output) to stdout.
- A line starting with `#` or a blank line is ignored.

### Script language
All coordinates are floating point numbers. Polygon names match `[A-Za-z][A-Za-z0-9_]*`. Polygons defined by the script are convex and given in counter-clockwise order.

- `POLY <name> <x1> <y1> <x2> <y2> ... <xn> <yn>` — store a polygon with the given vertex coordinates (at least 3 vertices, so at least 6 numbers).
  - Output: `POLY <name> <vertexCount>`
- `OVERLAP <a> <b>` — test whether convex polygons `<a>` and `<b>` overlap using `Intersector.overlapConvexPolygons`.
  - Output: `OVERLAP <a> <b> true` or `OVERLAP <a> <b> false`
- `CONTAINS <name> <x> <y>` — test whether point `(x, y)` is inside polygon `<name>` using `Intersector.isPointInPolygon` on the stored vertex array.
  - Output: `CONTAINS <name> <x> <y> true` or `CONTAINS <name> <x> <y> false`
  - Print the input coordinates back exactly as they appeared in the script line (preserve the formatting tokens the user typed).
- `SEGMENTS <ax> <ay> <bx> <by> <cx> <cy> <dx> <dy>` — intersect segment `AB` with segment `CD` using `Intersector.intersectSegments(Vector2, Vector2, Vector2, Vector2, Vector2)`.
  - If the segments meet, output: `SEGMENTS hit <px> <py>` where `<px>` and `<py>` are the intersection point coordinates rounded to **three decimal places** using `Locale.ROOT` (use `String.format(Locale.ROOT, "%.3f", value)`).
  - Otherwise, output: `SEGMENTS miss`
- `AREA <name>` — compute the signed area of polygon `<name>` via `Polygon.area()`.
  - Output: `AREA <name> <value>` where `<value>` is rounded to three decimal places using `Locale.ROOT`.

An unknown command, a reference to an undefined polygon, or a malformed numeric token must cause the program to print `ERROR <line-number> <command-or-reason>` (1-based line number counting blank and comment lines) and continue to the next line.

## Implementation Hints
- The `gdx-backend-headless` artifact pulls in the desktop natives loader, so a plain Gradle `application` plugin run is sufficient — no LWJGL window is required.
- `Intersector.overlapConvexPolygons` accepts `com.badlogic.gdx.math.Polygon` instances. `Intersector.isPointInPolygon` takes a `float[]` (or `(float[] polygon, int offset, int count, float x, float y)`).
- `HeadlessApplicationConfiguration.updatesPerSecond = 0` runs the loop as fast as possible; you can do all your work in `create()` and then call `Gdx.app.exit()`.
- Read the script through `Gdx.files.absolute(path)` or plain `java.nio.file.Files` — both work under the headless `Files` implementation.
- Make sure your `run.sh` filters out Gradle's own logging (e.g. use `./gradlew -q` or capture only the launcher's stdout) so that the only lines emitted are the result lines defined above.
- The headless launcher runs the `ApplicationListener` on its own thread; wait for the main loop to finish before exiting `main()` so all output is flushed.

## Acceptance Criteria
- Project path: /home/user/myproject
- Command: `bash run.sh <input-script-path>`
- Input argument format: `<input-script-path>` is the absolute path to a UTF-8 text file that conforms to the script language above.
- The stdout must contain exactly one result line per non-empty, non-comment input line, in the order the commands appear.
- All result lines must follow the exact formats listed in Requirements (including the literal command keyword in upper-case).
- The program must exit with status code 0 for any well-formed script.
- The implementation must use `com.badlogicgames.gdx:gdx-backend-headless:1.14.2` and `com.badlogicgames.gdx:gdx:1.14.2`, and must run under `HeadlessApplication`.
- Floating-point output must use `Locale.ROOT` and `%.3f` (three fractional digits, `.` as the decimal separator).
- `run.sh` must not print any output other than the program's stdout lines (Gradle banners, daemon notices, and progress logs must be suppressed).
- The build must be reproducible offline once dependencies are resolved on the first run (subsequent invocations must not require network access).

9:T15b7,# Procedural Pixmap Renderer with libGDX Headless Backend

## Background
libGDX exposes a CPU-side image type called `Pixmap` plus a small writer called `PixmapIO` that can emit uncompressed PNG files. Combined with the `gdx-backend-headless` back-end, these primitives can run inside a CI job, a server process, or a unit test — no OpenGL context is required as long as the code never touches `Gdx.gl*`.

Your job is to build a small, reproducible command-line tool that boots a `HeadlessApplication`, draws a procedural image into a `Pixmap` based on a text command script, and writes the result to a PNG file using `PixmapIO.writePNG`.

## Requirements
- Implement a Gradle-based Java project that depends on `com.badlogicgames.gdx:gdx:1.14.2`, `com.badlogicgames.gdx:gdx-backend-headless:1.14.2`, and the desktop natives classifier `com.badlogicgames.gdx:gdx-platform:1.14.2:natives-desktop`.
- Provide an `ApplicationListener` (e.g., extending `ApplicationAdapter`) that is started by a `HeadlessApplication` and performs all rendering and file I/O work inside the libGDX life-cycle (not from `main` directly).
- Read a plain-text command script (referred to as the *input file*) and execute its drawing commands against a single `Pixmap`. The script has one command per line; tokens are whitespace separated; blank lines and lines starting with `#` are ignored. Supported commands are:
  - `SIZE <width> <height>` — must be the first non-comment, non-blank line. Allocates a `Pixmap` of the given dimensions in `RGBA8888` format. The width and height are positive integers.
  - `FILL <r> <g> <b> <a>` — sets the current color from the 0–255 components and fills the entire pixmap with it.
  - `RECT <x> <y> <w> <h> <r> <g> <b> <a>` — sets the color and draws a filled axis-aligned rectangle with the libGDX `fillRectangle` helper.
  - `LINE <x1> <y1> <x2> <y2> <r> <g> <b> <a>` — sets the color and draws a 1-pixel-wide line using `drawLine`.
  - `CIRCLE <cx> <cy> <radius> <r> <g> <b> <a>` — sets the color and draws a filled circle with `fillCircle`.
  - `PIXEL <x> <y> <r> <g> <b> <a>` — sets the color and writes a single pixel via `drawPixel`.
- Write the final pixmap to the requested output path as a PNG using `PixmapIO.writePNG`. The output path is taken from the second CLI argument.
- After the headless run finishes, print exactly one summary line to stdout in the format `RENDER_OK width=<w> height=<h> commands=<n>`, where `<n>` is the number of drawing commands processed (excluding `SIZE`, comments, and blank lines).
- Expose a single shell entrypoint `run.sh` at the project root that accepts two positional arguments — the input command file and the output PNG path — and runs the headless launcher with them. The Docker image already provides every libGDX dependency in the local Gradle cache, so the script can (and should) run Gradle with `--offline` for speed.

## Implementation Hints
- Boot libGDX with `new HeadlessApplication(listener, config)` where `config` is a `HeadlessApplicationConfiguration` — the headless back-end automatically calls `HeadlessNativesLoader.load()` so `Pixmap` and `PixmapIO` are usable.
- All Pixmap work and the `PixmapIO.writePNG` call must happen on the libGDX thread (e.g., inside `create()` or via `Gdx.app.postRunnable`). After the work is queued/finished, call `Gdx.app.exit()` and `join()` the application thread before exiting `main`, otherwise the JVM may print the summary before the file is flushed.
- `Pixmap.setColor(r, g, b, a)` expects normalized floats in `[0, 1]`; the input file provides integer 0–255 values, so convert them before calling `setColor`.
- Resolve the input/output paths with `Gdx.files.absolute(...)` (or `Gdx.files.local(...)`) and pass the resulting `FileHandle` to `PixmapIO.writePNG`.
- Configure the Gradle build to produce a fat/runnable JAR or to be invokable via `./gradlew --no-daemon --offline run --args="..."`; `run.sh` can dispatch to whichever you choose as long as the two positional arguments are forwarded correctly.
- Always `dispose()` the `Pixmap` and let `HeadlessApplication` shut down cleanly so the PNG bytes are fully written.

## Acceptance Criteria
- Project path: /home/user/pixmap-renderer
- Command: `bash run.sh <input_file> <output_png>` (must be runnable from `/home/user/pixmap-renderer`).
- The command must boot the libGDX headless back-end and execute all draw commands through `Pixmap` / `PixmapIO` (no separate image-processing library).
- After a successful run:
  - A valid PNG file must exist at the path passed as the second argument.
  - The PNG dimensions must equal the `width` and `height` from the `SIZE` directive in the input file.
  - The pixel data must reflect the drawing commands executed in script order (later commands overwrite earlier ones).
  - The process must print exactly one line to stdout matching the regex `^RENDER_OK width=\d+ height=\d+ commands=\d+$`, where `commands` equals the number of drawing commands processed (excluding `SIZE`, comments, and blank lines).
  - The process must exit with status code `0`.
- The Gradle build must declare `com.badlogicgames.gdx:gdx:1.14.2`, `com.badlogicgames.gdx:gdx-backend-headless:1.14.2`, and the `natives-desktop` classifier of `com.badlogicgames.gdx:gdx-platform:1.14.2` (as a `runtimeOnly` or equivalent configuration).
- The Docker image already pre-populates the Gradle cache at `/home/user/.gradle` with the libGDX artifacts, so the build can run with `gradle --offline` for fast, deterministic dependency resolution.

a:T1a48,# libGDX Pooled Projectile Simulation (Headless Backend)

## Background
libGDX ships a non-rendering `gdx-backend-headless` backend that hosts an `ApplicationListener` outside of any OpenGL context. It is the canonical way to run libGDX game logic deterministically in CI (servers, simulations, automated tests). On top of the headless application, libGDX exposes performance-critical utilities such as `com.badlogic.gdx.utils.Pool<T>` (for object recycling) and `com.badlogic.gdx.math.Vector2` (for 2D math).

Your job is to build a deterministic projectile-physics simulation that runs under `HeadlessApplication`, recycles projectile instances through a libGDX `Pool`, and emits a fully reproducible transcript to a log file.

## Requirements
- Implement an `ApplicationListener` (an `ApplicationAdapter` subclass is fine) that drives the entire simulation in `create()` / `render()` / `dispose()` and exits via `Gdx.app.exit()` when the scheduled number of ticks has been processed.
- Boot the listener with `com.badlogic.gdx.backends.headless.HeadlessApplication` using `HeadlessApplicationConfiguration` (no LWJGL3 / no graphics backend allowed).
- Read the scenario from a text file that the user supplies on the command line and read it through libGDX (`Gdx.files.absolute(...)`).
- Represent every projectile with a Java class whose mutable state (id, position, velocity) is stored in libGDX `Vector2` instances and that is recycled through a `com.badlogic.gdx.utils.Pool<Projectile>` (the projectile class must implement `Pool.Poolable`).
- Run the simulation with a fixed integer tick (`delta = 1.0` per `render()` call). On every tick:
  1. Spawn every projectile scheduled for the current tick.
  2. For each active projectile (in spawn-id order), apply gravity to its velocity and then add the velocity to its position.
  3. After integration, any projectile whose `y <= floor_y` is grounded; print a grounded line, free it back to the pool, and remove it from the active list.
- Track pool reuse: the simulation must report the maximum number of simultaneously-active projectiles ever reached and the number of free instances sitting in the pool at the end of the run.
- Write every log line to the output file path supplied on the command line. Each line must end with a single `\n`.
- The task executor is responsible for writing the runnable Gradle project (`settings.gradle`, root `build.gradle`, the headless module with the `application` plugin, the Java sources, and a `gradle/wrapper` populated by `gradle wrapper`). The runnable entrypoint MUST be `/home/user/projectile_sim/run.sh`, a bash script that accepts the two CLI flags described under *Acceptance Criteria* and forwards their values into the launcher.

## Implementation Hints
- Pin libGDX to version `1.14.2`. The headless module needs `com.badlogicgames.gdx:gdx`, `com.badlogicgames.gdx:gdx-backend-headless`, and `com.badlogicgames.gdx:gdx-platform:1.14.2:natives-desktop`.
- A `HeadlessApplicationConfiguration` with `updatesPerSecond = 0` gives you maximum throughput, which is appropriate for a CI simulation. Whichever rate you pick, advance the simulation by exactly one tick per `render()` invocation; do not rely on `Gdx.graphics.getDeltaTime()` for the physics step.
- `HeadlessApplication` is constructed on its own thread. Pass file paths into your `ApplicationListener` constructor before booting the application so the listener can open the files inside `create()`.
- Use `com.badlogic.gdx.utils.Pool` (parameterised with your projectile type) and call `pool.obtain()` / `pool.free(p)`. `pool.getFree()` returns the number of unused instances currently stored in the pool – use it for the final summary.
- All numeric values in the scenario and in the log must be printed as Java `int`s (no decimal point, no trailing zeros). The simulation only needs integer arithmetic on `Vector2` components, so cast with `(int)` when printing.
- Make sure the program exits cleanly: call `Gdx.app.exit()` from inside `render()` once the requested number of ticks has been processed, then close any open writers in `dispose()`. The launcher main must `join` the headless application thread before returning so that the JVM does not exit while the log is still being flushed.

## Acceptance Criteria
- Project path: `/home/user/projectile_sim`
- Command: `bash /home/user/projectile_sim/run.sh --scenario <scenario_path> --output <output_path>`
  - `run.sh` MUST live at `/home/user/projectile_sim/run.sh`. It receives exactly the four positional tokens `--scenario <scenario_path> --output <output_path>` from the verifier (space-separated) and is responsible for delegating to the Gradle build (e.g. `./gradlew --no-daemon -q :headless:run --args="..."`). The internal launcher may parse the flags in any equivalent form.
  - The command MUST exit with status `0` on success.
- Scenario file format (UTF-8, one directive per line, `#` lines and blank lines ignored):
  - `TICKS <n>` (required, exactly once): the number of `render()` ticks to run.
  - `GRAVITY <gx> <gy>` (required, exactly once): integer acceleration applied to every active projectile's velocity at the start of each tick.
  - `FLOOR <y>` (required, exactly once): integer floor; any projectile with `y <= floor` after the integration step is grounded this tick.
  - `SPAWN <tick> <x> <y> <vx> <vy>` (zero or more): an integer projectile spawn. Ids are assigned in the order `SPAWN` lines appear in the file, starting at `0`.
- Output file format (written to the path passed via `--output`). For tick numbering, the first `render()` call is tick `0`.
  - Header line: `TICKS <n>` (echo of the scenario's tick count).
  - For every tick `t` in `[0, n)`:
    1. `TICK <t> ACTIVE <count>` – number of projectiles active at the END of the tick.
    2. For each active projectile at the END of the tick, in ascending spawn-id order: `P<id> x=<x> y=<y> vx=<vx> vy=<vy>` (all integers).
    3. For each projectile grounded during this tick, in ascending spawn-id order: `GROUNDED P<id> tick=<t>`.
  - Final summary line: `SUMMARY spawned=<S> grounded=<G> pool_free=<F> peak_active=<P>` where:
    - `S` is the total number of `SPAWN` directives processed.
    - `G` is the total number of grounded projectiles.
    - `F` is `pool.getFree()` at the end of the run.
    - `P` is the maximum number of projectiles ever simultaneously active during the simulation.
- A scenario that contains no `SPAWN` lines must still emit the header, one `TICK <t> ACTIVE 0` line per tick, and the final summary with all counters set to `0`.
- The simulation must use the headless backend (`com.badlogic.gdx.backends.headless.HeadlessApplication`). The graphical LWJGL3 backend is forbidden.

b:T192b,# Scene2D Action Replay under libGDX Headless Backend

## Background
libGDX exposes a non-rendering `gdx-backend-headless` module (`com.badlogicgames.gdx:gdx-backend-headless`) that boots a real `HeadlessApplication` thread without an OpenGL context, making it suitable for servers, CI runners, and deterministic simulations. Scene2D's `Actor`, `Group`, and `Action` classes are pure scene-graph logic — they do not invoke OpenGL until you call `draw(...)` — so they can be exercised under the headless backend if the implementation is careful not to touch `Gdx.gl*` or `SpriteBatch`.

Your task is to build a small libGDX project that loads a Scene2D scene description from a text file, queues `Action`s on the actors, advances the scene tick-by-tick on the headless main loop, and writes each actor's final 2D position to an output file.

## Requirements
- Create a Gradle (single-module) Java project at `/home/user/gdx-stage-sim`.
- Use libGDX `1.14.2` with the headless backend (`com.badlogicgames.gdx:gdx-backend-headless:1.14.2`) on top of `com.badlogicgames.gdx:gdx:1.14.2` and the `com.badlogicgames.gdx:gdx-platform:1.14.2:natives-desktop` natives classifier.
- Bootstrap the application from a `main` method that constructs a `com.badlogic.gdx.backends.headless.HeadlessApplication` driving an `ApplicationListener` (or `ApplicationAdapter`).
- Use Scene2D `com.badlogic.gdx.scenes.scene2d.Actor` instances and Scene2D `Action`s (`com.badlogic.gdx.scenes.scene2d.actions.MoveByAction`, the `Actions` helper, or equivalent) to model and update each actor's position.
- Read the script file via `Gdx.files.absolute(...)` in `create()`, register the requested actors and queue their `Action`s, run the requested number of ticks by advancing each actor's action queue by `dt` seconds per `render()` call, and write the output file from `dispose()` before the JVM exits.
- Make the program runnable through Gradle's `application` plugin so it can be launched with `./gradlew run --args="<script-path> <output-path>"`.

## Implementation Hints
- The headless backend swaps `Gdx.graphics`/`Gdx.audio`/`Gdx.input` for mocks, so **do not** call anything under `Gdx.gl*` and do **not** call `SpriteBatch`/`Stage.draw(...)` — those require a real GL context. Scene2D's `Actor.act(float)`, `Group.act(float)`, and `Action.act(float)` are pure CPU logic and are safe to call under headless.
- Use `HeadlessApplicationConfiguration.updatesPerSecond = 0` so the main loop ticks as fast as possible and the simulation is not throttled by wall-clock pacing.
- `Gdx.app.exit()` is asynchronous (it posts a runnable that flips `running` and then triggers `pause()` + `dispose()`). To guarantee that the output file is flushed before the JVM exits, write the file from `dispose()` and `join()` the `HeadlessApplication` main loop thread from `main`.
- The simulation step must be deterministic and independent of wall-clock time: use the `dt` value parsed from the script file, **not** `Gdx.graphics.getDeltaTime()`.
- Use `Locale.ROOT` (or equivalent) when formatting floating-point output so the decimal separator is always `.`.
- Look at `HeadlessApplication`, `HeadlessApplicationConfiguration`, Scene2D `Actor`/`Action`, and `MoveByAction` in the libGDX javadoc / source for the contracts you need to satisfy.

## Acceptance Criteria
- Project path: /home/user/gdx-stage-sim
- Command: `./gradlew --no-daemon -q run --args="<script-path> <output-path>"` from `/home/user/gdx-stage-sim`
- The command must terminate on its own (no manual kill) with exit code 0.

### Script file format (`<script-path>`)
- UTF-8 text file, one directive per line, LF line endings.
- Lines starting with `#` are comments and are skipped.
- Blank or whitespace-only lines are skipped.
- Tokens on a line are separated by one or more spaces or tabs; leading/trailing whitespace is trimmed.
- Numeric values use a `.` decimal separator and no thousands separators.
- The following directives MUST be supported. The first token is the directive name and is case-insensitive (e.g., `DT`, `Dt`, and `dt` are equivalent):
  - `dt <float>` — fixed simulation time step in seconds (`> 0`). Must appear exactly once before any actor-related directive.
  - `ticks <int>` — total number of `render()` ticks to run (`>= 0`). Must appear exactly once before any actor-related directive.
  - `actor <id> <x> <y>` — create an `Actor` with identifier `<id>` (alphanumeric/underscore, case-sensitive, unique within the file) at initial position `(<x>, <y>)`. The order of `actor` directives in the file defines the output order.
  - `moveby <id> <dx> <dy> <duration>` — queue a Scene2D `MoveByAction` (default linear interpolation) on the previously declared actor `<id>` that moves it by `(<dx>, <dy>)` over `<duration>` seconds (`>= 0`). At most one `moveby` directive may target any given actor in a given script.

### Output file format (`<output-path>`)
- The program creates or overwrites the file.
- UTF-8 encoding, LF line endings, one `<id>=<x>,<y>` line per declared actor, in the order the actors were declared in the script.
- Each `<x>` and `<y>` is the actor's final position formatted with `%.6f` using `Locale.ROOT`.

### Simulation semantics
- After `create()` completes, the listener performs exactly `ticks` ticks. On each tick it advances every actor's action queue by `dt` seconds (e.g., via `Actor.act(dt)` or `Group.act(dt)`). After the final tick the listener triggers application exit so the headless main loop terminates.
- `ticks=0` is a valid input and must produce the initial positions unchanged in the output file.
- An `actor` declared with no `moveby` directive must remain at its initial position throughout the simulation.
- A `MoveByAction` of duration `D` reaches the full `(dx, dy)` displacement once the simulated time elapsed (`accumulated_dt`) reaches `D`; any remaining ticks must not move that actor further (Scene2D actions auto-remove on completion).
- The output file must be flushed and the JVM must terminate within 60 seconds for any input with `ticks <= 50000`.

### Implementation constraints (observable)
- The build must declare a dependency on `com.badlogicgames.gdx:gdx-backend-headless:1.14.2`.
- At least one source file in the project must reference `HeadlessApplication` (the headless backend bootstrap class).
- At least one source file must reference the Scene2D package (`com.badlogic.gdx.scenes.scene2d`).

c:T1869,# libGDX Headless: Scripted Keystroke Replay Walker

## Background
libGDX ships a non-rendering backend (`com.badlogicgames.gdx:gdx-backend-headless`) that swaps the live `Graphics`/`Input`/`Audio` implementations for mocks so that a libGDX `ApplicationListener` can be driven from a server, CI runner, or test harness. The bundled `MockInput` simply returns zero/`false` for every method, which makes it useless on its own for any input-driven game. A common pattern is to subclass `MockInput`, read a sequence of keystrokes from a text file, and re-dispatch them to the live `InputProcessor` each tick so that a deterministic replay of player input can drive the game from a script.

In this task you will build a small libGDX 1.14.2 project around the **headless** backend that simulates a 2D grid walker. The walker reads a keystroke replay file, advances one keystroke per render tick by feeding it through a custom `MockInput` subclass, and prints its final position once every scripted input has been consumed.

## Requirements
- Create a Gradle (Java 17) project at `/home/user/gdx-game` that depends on libGDX `1.14.2` (`gdx`, `gdx-backend-headless`, `gdx-platform:natives-desktop`).
- Implement a libGDX `ApplicationListener` (or `ApplicationAdapter`) that maintains a 2D integer walker position, starting at `(0, 0)`.
- Implement a custom `MockInput` subclass that reads a keystroke sequence from a text file and dispatches exactly one `InputProcessor.keyDown(keycode)` event per render tick. After the file is exhausted, the application must call `Gdx.app.exit()` so the headless main loop terminates cleanly.
- Drive position updates through libGDX's `InputProcessor` contract (i.e., the game must register an `InputProcessor` via `Gdx.input.setInputProcessor(...)`, and position updates happen inside that processor's callbacks).
- Produce a runnable launcher (using the Gradle `application` plugin) at `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game` that accepts a `--input=<file>` argument and runs the simulation on a `HeadlessApplication`.
- Print the final walker position to stdout in the format `Final position: (<x>, <y>)`.

### Input file format
- UTF-8 text file.
- One keystroke per line. Lines are case-insensitive and trimmed of surrounding whitespace.
- Blank lines are skipped (no tick is consumed).
- Lines starting with `#` are treated as comments and skipped.
- Allowed keystroke names: `UP`, `DOWN`, `LEFT`, `RIGHT`. Any other non-blank/non-comment token must cause the program to print `Error: unknown key <token>` to stderr and exit with a non-zero status code (do **not** print a `Final position:` line in that case).

### Movement semantics
- `UP` increments y by 1.
- `DOWN` decrements y by 1.
- `RIGHT` increments x by 1.
- `LEFT` decrements x by 1.
- Each line consumes exactly one render tick. The position update **must** happen inside the registered `InputProcessor.keyDown(int keycode)` callback (use `com.badlogic.gdx.Input.Keys.UP|DOWN|LEFT|RIGHT`).

## Implementation Hints
- Pin `gdxVersion=1.14.2`. The required Maven coordinates and the headless backend recipe are summarised in the libGDX docs ([Dependency management with Gradle](https://libgdx.com/wiki/articles/dependency-management-with-gradle), [Headless backend source](https://github.com/libgdx/libgdx/blob/master/backends/gdx-backend-headless/src/com/badlogic/gdx/backends/headless/HeadlessApplication.java)).
- The Gradle `application` plugin gives you a turnkey launcher: configure `mainClass` and run `./gradlew installDist` to produce `build/install/<projectName>/bin/<projectName>`.
- `HeadlessApplication` runs the listener on its own thread, so block the launcher's `main` thread (e.g., by joining the headless main-loop thread) until the application has exited; otherwise the JVM may quit before the simulation finishes.
- `HeadlessApplication` constructs and assigns a no-op `MockInput` to `Gdx.input` in its constructor. After constructing the application, overwrite `Gdx.input` with your scripted subclass so that the rest of the framework sees it. Your subclass should remember the `InputProcessor` passed to `setInputProcessor(...)` and dispatch `keyDown` events from `render()` (via a `Gdx.app.postRunnable(...)` or directly from the simulation tick).
- Consider using `HeadlessApplicationConfiguration.updatesPerSecond` to throttle the loop so the test does not spin the CPU.
- The headless backend is sensitive to OpenGL calls — do **not** touch `Gdx.gl*` anywhere in your code.
- Bootstrap the Gradle wrapper with `gradle wrapper --gradle-version 8.10 --distribution-type bin` so reproducible builds are possible from the project root.

## Acceptance Criteria
- Project path: `/home/user/gdx-game`.
- After running `./gradlew --no-daemon --quiet installDist` from the project root, the launcher script `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game` must exist and be executable.
- Command: `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game --input=<path>`
- The command must accept the `--input=<path>` argument (equals form) where `<path>` is the absolute path to a keystroke replay file conforming to the format above.
- Successful runs (every line parses as a valid key) must:
  - Print exactly one line `Final position: (<x>, <y>)` to stdout (e.g., `Final position: (1, -2)`), where `<x>` and `<y>` are the final walker coordinates after processing every scripted keystroke.
  - Exit with status code `0`.
- Runs where any non-comment, non-blank line contains a token outside `{UP, DOWN, LEFT, RIGHT}` (case-insensitive) must:
  - Print `Error: unknown key <token>` to stderr (substituting the offending raw token verbatim).
  - Not print a `Final position:` line.
  - Exit with a non-zero status code.
- Empty replay files (no non-blank, non-comment lines) must print `Final position: (0, 0)` and exit `0`.
- The application must use `com.badlogic.gdx.backends.headless.HeadlessApplication` to boot the listener, and the position must be updated from within an `InputProcessor.keyDown(int)` callback registered via `Gdx.input.setInputProcessor(...)`. (This is verified indirectly through the behavioural tests above and through a build that links against `gdx-backend-headless:1.14.2`.)

d:T1e3d,# libGDX Headless: 2D Shape Overlap Collision Report

## Background
libGDX ships a non-rendering backend (`com.badlogicgames.gdx:gdx-backend-headless`) that swaps the live `Graphics`/`Input`/`Audio` implementations for mocks so a libGDX `ApplicationListener` can be driven from a server, CI runner, or test harness. The framework's math package (`com.badlogic.gdx.math`) is fully usable from this backend and ships ready-made 2D collision primitives: `Rectangle.overlaps(Rectangle)`, `Circle.overlaps(Circle)`, and `Intersector.overlaps(Circle, Rectangle)`. Servers and offline batch jobs frequently lean on these primitives to detect collisions, validate level layouts, or pre-compute spatial indices without ever opening a window.

In this task you will build a small libGDX 1.14.2 project around the **headless** backend that consumes a 2D shape description file, computes every overlapping pair using libGDX's own geometry classes, and writes a sorted collision report to disk.

## Requirements
- Create a Gradle (Java 17) project at `/home/user/gdx-game` that depends on libGDX `1.14.2` (`gdx`, `gdx-backend-headless`, `gdx-platform:natives-desktop`).
- Implement a libGDX `ApplicationListener` (or `ApplicationAdapter`) that runs under `com.badlogic.gdx.backends.headless.HeadlessApplication`.
- Inside the listener's `create()` callback (or driven from `render()` on the very first tick) load the shape definition file via `Gdx.files.absolute(<path>)`, parse it, compute every overlapping pair using libGDX's math primitives, write the report file, and call `Gdx.app.exit()` so the headless main loop terminates cleanly.
- Pairwise overlap detection MUST use the libGDX classes `com.badlogic.gdx.math.Rectangle`, `com.badlogic.gdx.math.Circle`, and `com.badlogic.gdx.math.Intersector` (specifically `Rectangle.overlaps(Rectangle)`, `Circle.overlaps(Circle)`, and `Intersector.overlaps(Circle, Rectangle)`). Do **NOT** re-implement the geometry checks by hand.
- Produce a runnable launcher (via the Gradle `application` plugin) at `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game` that accepts two arguments: `--shapes=<input_path>` and `--output=<output_path>`, both absolute paths.

### Input file format
- UTF-8 text file.
- Lines starting with `#` are comments and are skipped.
- Blank/whitespace-only lines are skipped.
- Each non-comment, non-blank line defines exactly one shape using whitespace as the delimiter:
  - Rectangle: `<id> rect <x> <y> <width> <height>`
  - Circle:    `<id> circle <x> <y> <radius>`
- `<id>` is a non-empty token consisting of letters, digits, underscores, or hyphens (no whitespace). IDs are case-sensitive and must be unique within a file; on a duplicate ID the program must print `Error: duplicate id <id>` to stderr and exit with a non-zero status code.
- Numeric coordinates and dimensions are decimal floating-point numbers parseable by `Float.parseFloat`. `width`, `height`, and `radius` must be strictly positive.
- Any malformed line (unknown shape keyword, wrong number of tokens, non-positive size, unparseable number) must cause the program to print `Error: invalid shape line: <line>` (substituting the offending raw line verbatim, without the trailing newline) to stderr and exit with a non-zero status code.

### Output file format
- UTF-8 text file written to the path supplied via `--output=<path>`. If the file already exists it must be overwritten.
- One overlapping pair per line, formatted exactly as `<id_a>\t<id_b>` (tab-separated). Within each pair the lexicographically smaller ID (using Java string `compareTo`) MUST appear first.
- Lines MUST be sorted lexicographically: primary key is the first id (`id_a`), secondary key is the second id (`id_b`).
- A final summary line `total_overlaps=<N>` MUST be appended after the pair listing, where `<N>` is the total number of distinct overlapping pairs.
- If no shapes overlap, the file must contain a single line `total_overlaps=0`.
- The file MUST end with a single trailing newline.

### Overlap semantics
- Two rectangles overlap iff `Rectangle.overlaps(Rectangle)` returns `true` (strict, edge-touching does NOT count).
- Two circles overlap iff `Circle.overlaps(Circle)` returns `true` (strict distance check, tangent circles do NOT count).
- A circle and a rectangle overlap iff `Intersector.overlaps(Circle, Rectangle)` returns `true`.
- Each unordered pair is reported at most once.

## Implementation Hints
- Pin `gdxVersion=1.14.2`. The required Maven coordinates and the headless backend recipe are summarised in the libGDX docs ([Dependency management with Gradle](https://libgdx.com/wiki/articles/dependency-management-with-gradle), [Headless backend source](https://github.com/libgdx/libgdx/blob/master/backends/gdx-backend-headless/src/com/badlogic/gdx/backends/headless/HeadlessApplication.java)).
- The Gradle `application` plugin gives you a turnkey launcher: configure `mainClass` and run `./gradlew installDist` to produce `build/install/<projectName>/bin/<projectName>`.
- `HeadlessApplication` boots its `ApplicationListener` on its own thread; the launcher's `main` thread should wait for the listener to call `Gdx.app.exit()` (e.g., by joining the headless main-loop thread) before exiting, otherwise the JVM may quit before the output file is flushed.
- Use `HeadlessApplicationConfiguration.updatesPerSecond = 0` (or a small positive number) so the main loop does not busy-wait; `create()` runs on the very first tick.
- The headless backend is sensitive to OpenGL calls — do **NOT** touch `Gdx.gl*` anywhere in your code.
- Bootstrap the Gradle wrapper with `gradle wrapper --gradle-version 8.10 --distribution-type bin` so reproducible builds are possible from the project root.
- libGDX's geometry classes live in `com.badlogic.gdx.math`. `Rectangle` is constructed as `new Rectangle(x, y, width, height)`, `Circle` as `new Circle(x, y, radius)`. The mixed circle-vs-rectangle overlap check is provided by `Intersector.overlaps(Circle, Rectangle)`.
- Report parse/validation errors via stderr (`System.err.println(...)` or `Gdx.app.error(...)` plus a non-zero exit code from the launcher's `main`). Successful runs must exit with status code `0`.

## Acceptance Criteria
- Project path: `/home/user/gdx-game`.
- After running `./gradlew --no-daemon --quiet installDist` from the project root, the launcher script `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game` must exist and be executable.
- Command: `/home/user/gdx-game/build/install/gdx-game/bin/gdx-game --shapes=<input_path> --output=<output_path>`
- The command must accept the `--shapes=<path>` and `--output=<path>` arguments (equals form) where both paths are absolute.
- Successful runs (every line parses and every ID is unique) must:
  - Create or overwrite the file at `<output_path>` with the format described above.
  - Exit with status code `0`.
  - Produce no `Error:` line on stderr.
- Runs where the input contains a malformed shape line must:
  - Print `Error: invalid shape line: <line>` to stderr (substituting the offending raw line verbatim).
  - Exit with a non-zero status code.
  - Not create or overwrite `<output_path>` with a successful report.
- Runs where the input contains a duplicate ID must:
  - Print `Error: duplicate id <id>` to stderr (substituting the offending ID verbatim).
  - Exit with a non-zero status code.
- The application must use `com.badlogic.gdx.backends.headless.HeadlessApplication` to boot the listener; pairwise overlap detection must use libGDX's `Rectangle.overlaps`, `Circle.overlaps`, and `Intersector.overlaps(Circle, Rectangle)`. (This is verified indirectly through the behavioural tests and through a build that links against `gdx-backend-headless:1.14.2`.)

e:T1b7b,# Headless libGDX Turn-Based Command Loop

## Background
You are building the deterministic core of a tiny dungeon-crawler that runs entirely on the libGDX **headless** backend (no GPU, no window). The same engine will later be reused inside the LWJGL3 desktop client, but for this task it must run in a server / CI context. The game advances one *turn* per render tick, consuming a single command from a script file, mutating its in-memory world, and appending a single transcript line per turn.

libGDX is a Java game framework. The `gdx-backend-headless` module ships `HeadlessApplication`, `HeadlessFiles`, `MockGraphics`, `MockAudio`, and `MockInput` so the rest of the library works without OpenGL. You will rely on this backend together with `Gdx.files`, `Gdx.app`, an `ApplicationListener`, and a custom `MockInput` subclass.

## Requirements
- Set up a multi-module Gradle project with at least:
  - a shared `core` module that contains the `ApplicationListener` (game logic) and the `MockInput` subclass,
  - a `headless` module whose `main(String[])` boots a `HeadlessApplication` and wires up command-file input.
- Pin `gdxVersion = 1.14.2` and depend on `com.badlogicgames.gdx:gdx`, `com.badlogicgames.gdx:gdx-backend-headless`, and `com.badlogicgames.gdx:gdx-platform:...:natives-desktop`.
- The headless launcher must accept three CLI arguments (any order):
  - `--map=<absolute_path>`
  - `--commands=<absolute_path>`
  - `--transcript=<absolute_path>`
- The map file uses this UTF-8 text format (whitespace-separated tokens, `#` lines and blank lines ignored):
  ```
  <WIDTH> <HEIGHT>
  <PLAYER_X> <PLAYER_Y>
  <ITEM_COUNT>
  <X1> <Y1> <NAME1>
  <X2> <Y2> <NAME2>
  ...
  ```
  Coordinates use the origin at the lower-left: x grows east, y grows north. Item names are single tokens (no spaces).
- The commands file holds one command per line. Valid commands (case-sensitive):
  - `N` / `S` / `E` / `W` — move the player one cell north / south / east / west.
  - `PICK` — pick up exactly one item located at the player's current cell (if multiple, the one defined earliest in the map file; if none, no-op).
  - `QUIT` — stop the loop after recording this turn.
  - Any other non-empty, non-blank token is an *unknown* command. Trim each line; ignore blank lines and lines starting with `#`.
- Movement that would leave the map (`x` outside `[0, WIDTH)` or `y` outside `[0, HEIGHT)`) is rejected: the player stays in place but the turn still counts.
- A picked-up item is removed from the world; subsequent `PICK` calls on the same cell only succeed if another item was defined there.
- The transcript file must be created (overwriting any pre-existing file) and contain one line per executed turn, terminated by a single `FINAL` line:
  - Per-turn format (no extra spaces, exact tokens):
    ```
    turn=<N> cmd=<RAW_COMMAND> pos=<X>,<Y> inv=<NAMES>
    ```
    where `<N>` starts at 1 and increments by one per turn, `<RAW_COMMAND>` is the original (trimmed) token from the command file, `<X>,<Y>` is the player's position **after** the command resolves, and `<NAMES>` is the inventory rendered as a comma-separated list of item names in pickup order (empty string if the inventory is empty, e.g. `inv=`).
  - Final line format (after the last turn):
    ```
    FINAL pos=<X>,<Y> inv=<NAMES> turns=<TOTAL_TURNS>
    ```
- The loop ends when **either** a `QUIT` command has just been recorded, **or** the command file is exhausted. In both cases the program must call `Gdx.app.exit()`, flush the transcript, and let the JVM terminate cleanly with exit code `0`.
- Input must be delivered to the game via a custom subclass of `com.badlogic.gdx.backends.headless.mock.input.MockInput`. The subclass must hold the queued commands derived from the input file and, each tick, expose the *current* command to the listener via `isKeyJustPressed(int)` / `isKeyPressed(int)`. Replace `Gdx.input` with this subclass right after constructing the `HeadlessApplication`.
- The mapping from command to libGDX keycode (`com.badlogic.gdx.Input.Keys`) must be:
  - `N` → `Input.Keys.UP`
  - `S` → `Input.Keys.DOWN`
  - `E` → `Input.Keys.RIGHT`
  - `W` → `Input.Keys.LEFT`
  - `PICK` → `Input.Keys.SPACE`
  - `QUIT` → `Input.Keys.ESCAPE`
  - any other command → no keycode active for this tick (the game still records the raw command in the transcript without changing state).
- The map file is read with `Gdx.files.absolute(...)`. The transcript is written using `Gdx.files.absolute(...).writeString(...)` (or any equivalent libGDX `FileHandle` API).
- The headless `HeadlessApplicationConfiguration.updatesPerSecond` should be set so the loop runs as fast as possible (you may use `0` for unlimited, or a high value such as `1000`).
- The launcher main thread must wait for the headless main-loop thread to finish before returning, so the transcript is fully written by the time the process exits.

## Implementation Hints
- The headless backend can be started with `new HeadlessApplication(listener, config)`. Right after construction, `Gdx.input` is a vanilla `MockInput`; swap it for your scripted subclass before the first render tick fires.
- Keep all OpenGL-touching APIs (`SpriteBatch`, `Texture`, `Gdx.gl*`, etc.) out of the project — the headless backend will NPE if they are invoked.
- A simple way to advance one command per tick is to have the scripted `MockInput` expose a `tick()` method (or equivalent) and call it from the start of `ApplicationListener.render()` before reading `isKeyJustPressed`.
- Remember to handle the `QUIT` command *after* writing its transcript line, so the FINAL line reflects the state at the moment of quitting.
- `Gdx.files.absolute(path)` returns a `FileHandle` suitable for both reading and writing; for the transcript prefer a single `writeString(..., false)` call (or buffered append) so partial writes are not visible to the verifier.

## Acceptance Criteria
- Project path: `/home/user/turn-based-game`
- Command: `./gradlew --no-daemon -q :headless:run --args="--map=<MAP> --commands=<COMMANDS> --transcript=<TRANSCRIPT>"` (run from the project root, with absolute paths for all three arguments)
- Exit code: `0`
- The transcript file at the supplied `--transcript` path must:
  - exist after the command completes,
  - contain exactly `T + 1` lines, where `T` is the number of executed turns,
  - have one `turn=<N> cmd=<RAW> pos=<X>,<Y> inv=<NAMES>` line per turn in execution order,
  - end with a single `FINAL pos=<X>,<Y> inv=<NAMES> turns=<T>` line,
  - use exactly the field separators specified above (single spaces between fields, no trailing spaces, LF line endings).
- The headless backend (`gdx-backend-headless`) must be on the runtime classpath of the `:headless` module.
- The game's input must flow through a subclass of `com.badlogic.gdx.backends.headless.mock.input.MockInput`. (Verified indirectly via behavior: only the documented keycodes must drive state changes, while non-mapped commands must leave position and inventory unchanged.)

f:T1c67,# libGDX Headless XML Scene Graph Loader

## Background
This task is about building a deterministic, CI-runnable command-line tool with the libGDX framework. libGDX ships a non-rendering **headless backend** (`com.badlogicgames.gdx:gdx-backend-headless`) that boots an `ApplicationListener` without an OpenGL context, plus a `Gdx.files`-backed `FileHandle` API and an `XmlReader` DOM parser in `com.badlogic.gdx.utils`.

Your job is to implement a small libGDX 1.14.2 application that loads an XML file describing a 2D hierarchical **scene graph** (nodes with local transforms and children), uses `HeadlessApplication` to drive the lifecycle, parses the XML with libGDX's `XmlReader`, computes each node's **world-space transform** by composing the chain of parent transforms, and writes a deterministic, depth-first transcript of the resulting tree to an output file.

## Requirements
- Use libGDX `1.14.2` with the `gdx-backend-headless` backend. The program must run with no OpenGL context; it must not call `Gdx.gl*` or instantiate `SpriteBatch`/`Texture`.
- The project must be built and run with Gradle (the Gradle wrapper is already provided in the project).
- The entry point must be runnable through Gradle as `./gradlew --no-daemon :app:run --quiet --args="--scene=<scene_xml_path> --output=<output_txt_path>"`, where:
  - `<scene_xml_path>` is an absolute path to an input XML file.
  - `<output_txt_path>` is an absolute path to which the program must write the transcript.
- The program must boot a `HeadlessApplication` with `updatesPerSecond = 0` (run as fast as possible) and exit cleanly after the transcript is written (i.e., call `Gdx.app.exit()` and let the headless main loop join).
- File I/O on both the scene XML and the output transcript must go through `Gdx.files.absolute(...)` so the file backend is exercised in headless mode.
- The XML must be parsed with `com.badlogic.gdx.utils.XmlReader` (do not use `javax.xml`, DOM4J, JDOM, or other XML parsers).

## Input XML Format
The XML describes a single rooted scene. A node may have any number of child `<node>` elements.

```xml
<scene>
  <node name="root" x="10" y="20" rotation="0" scaleX="1" scaleY="1">
    <node name="child" x="5" y="0" rotation="45" scaleX="1" scaleY="1">
      <node name="leaf" x="3" y="3" rotation="0" scaleX="0.5" scaleY="0.5"/>
    </node>
  </node>
</scene>
```

Rules:
- The root element is `<scene>` and contains exactly one top-level `<node>`.
- Each `<node>` element must have a `name` attribute. The optional numeric attributes (`x`, `y`, `rotation`, `scaleX`, `scaleY`) default to `0`, `0`, `0`, `1`, `1` respectively when absent.
- `rotation` is expressed in **degrees** (counter-clockwise, libGDX convention).
- All node names within a single parent are unique; names contain only `[A-Za-z0-9_]`.

## Transform Composition Rules
For every node `N` with parent `P`, compute `N`'s world transform from its local `(x, y, rotation, scaleX, scaleY)` and `P`'s world transform as follows. The root node's parent is the identity transform (world position `(0,0)`, world rotation `0`, world scale `(1,1)`).

1. Scale the local offset by the parent's world scale: `lx = N.x * P.worldScaleX`, `ly = N.y * P.worldScaleY`.
2. Rotate the scaled offset by the parent's world rotation (in degrees, counter-clockwise): if `cos = cos(P.worldRotation)` and `sin = sin(P.worldRotation)`, then `rx = lx*cos - ly*sin`, `ry = lx*sin + ly*cos`.
3. Translate by the parent's world position: `N.worldX = P.worldX + rx`, `N.worldY = P.worldY + ry`.
4. Compose rotation: `N.worldRotation = P.worldRotation + N.rotation`.
5. Compose scale (component-wise): `N.worldScaleX = P.worldScaleX * N.scaleX`, `N.worldScaleY = P.worldScaleY * N.scaleY`.

Trigonometry must use degrees (you may use `com.badlogic.gdx.math.MathUtils.cosDeg/sinDeg` or `Math.cos`/`Math.sin` with manual degree-to-radian conversion).

## Output Transcript Format
The program must write the output file with these properties:
- One line per node, in **pre-order depth-first traversal** (each parent is emitted before its children). Children must be visited in the same order they appear in the XML.
- Each line has exactly the following six tab-separated fields, with values formatted using `%.4f` and `java.util.Locale.ROOT`:
  ```
  <dotted_path>\tworldX=<%.4f>\tworldY=<%.4f>\tworldRotationDeg=<%.4f>\tworldScaleX=<%.4f>\tworldScaleY=<%.4f>
  ```
  Where `<dotted_path>` is the path from the root node down to this node, with names joined by `.` (the root is its own name, e.g. `root`; the leaf in the sample above is `root.child.leaf`).
- Lines are terminated by a single `\n`. The file ends with a trailing newline after the last node.
- `worldRotationDeg` is the raw composed value (do **not** normalize to `[0, 360)`).
- The output file must be written through `Gdx.files.absolute(<output_txt_path>).writeString(transcript, false, "UTF-8")` (or equivalent libGDX FileHandle write).

## Implementation Hints
- Use `gdx-liftoff`-style multi-module Gradle layout is **not** required; a single `app` module that depends on `com.badlogicgames.gdx:gdx:1.14.2`, `com.badlogicgames.gdx:gdx-backend-headless:1.14.2`, and `runtimeOnly com.badlogicgames.gdx:gdx-platform:1.14.2:natives-desktop` is sufficient.
- The Gradle `application` plugin lets you wire `mainClass` and forward `--args` to `JavaExec`. Forward the program arguments to your `ApplicationListener` via constructor parameters before constructing `HeadlessApplication`.
- Use `com.badlogic.gdx.utils.XmlReader` and recurse on `XmlReader.Element#getChildrenByName("node")`. Use `Element#getFloatAttribute(name, defaultValue)` for the optional numeric attributes.
- Build the transcript in memory (e.g., in a `StringBuilder`) inside `create()`, write it through `Gdx.files.absolute(...)`, and call `Gdx.app.exit()`. Wait for the main loop thread by holding a reference to the `HeadlessApplication` instance (do not return from `main` before the application's main loop thread joins).
- Refer to the official docs for the headless backend, `XmlReader`, `Gdx.files`, and `MathUtils` (see the libGDX wiki and Javadoc).

## Acceptance Criteria
- Project path: /home/user/gdx-scene-loader
- Command: cd /home/user/gdx-scene-loader && ./gradlew --no-daemon :app:run --quiet --args="--scene=<scene_xml_path> --output=<output_txt_path>"
- The Gradle build must declare a dependency on `com.badlogicgames.gdx:gdx-backend-headless:1.14.2` and run the program through `HeadlessApplication`.
- The XML must be parsed with `com.badlogic.gdx.utils.XmlReader`.
- The output file at `<output_txt_path>` must exist after the command completes.
- The output file must contain one line per scene-graph node in pre-order DFS, using the exact format `<dotted_path>\tworldX=<%.4f>\tworldY=<%.4f>\tworldRotationDeg=<%.4f>\tworldScaleX=<%.4f>\tworldScaleY=<%.4f>` (tab-separated, locale-independent `.` decimal separator).
- Every numeric value must be rounded to 4 decimal places using `Locale.ROOT`.
- The number of output lines must equal the number of `<node>` elements in the input XML.
- The program must exit with status `0` when the run succeeds.
- The program must not invoke any OpenGL APIs (verified indirectly by running under the headless backend with no display).

0:{"buildId":"j7B674z9avb2eOKum1P_x","rsc":["$","$1","c",{"children":[["$","div",null,{"className":"min-h-screen bg-background text-foreground font-sans selection:bg-primary/20","children":[["$","div",null,{"className":"fixed inset-0 -z-10 h-full w-full bg-background bg-[radial-gradient(#2a2a2a_1px,transparent_1px)] [background-size:16px_16px] [mask-image:radial-gradient(ellipse_50%_50%_at_50%_50%,#000_70%,transparent_100%)] opacity-20 dark:opacity-40"}],["$","$2",null,{"fallback":["$","div",null,{"className":"container mx-auto py-8 text-center","children":"Loading tasks..."}],"children":["$","$L3",null,{"tasksData":[{"taskName":"ashley_ecs_simulation_headless","instruction":"$4","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"ashley_ecs_simulation_headless__poPxx46","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":315.925,"latency_breakdown":{"env_setup":33.241,"agent_setup":23.221,"agent_exec":230.359,"verifier":14.487},"taskName":"ashley_ecs_simulation_headless","jobId":"poPxx46","exec_duration":230.359},{"job_name":"2026-06-12__09-28-23","trial_name":"ashley_ecs_simulation_headless__KyCSTsF","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":false,"reward":0,"error":false,"latency_sec":591.575,"latency_breakdown":{"env_setup":27.413,"agent_setup":22.206,"agent_exec":510.207,"verifier":15.247},"taskName":"ashley_ecs_simulation_headless","jobId":"KyCSTsF","exec_duration":510.207},{"job_name":"2026-06-12__09-28-23","trial_name":"ashley_ecs_simulation_headless__zbsM9Sv","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":358.175,"latency_breakdown":{"env_setup":41.153,"agent_setup":23.479,"agent_exec":257.128,"verifier":15.501},"taskName":"ashley_ecs_simulation_headless","jobId":"zbsM9Sv","exec_duration":257.128},{"job_name":"2026-06-12__02-48-52","trial_name":"ashley_ecs_simulation_headless__Z74yNfA","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":false,"reward":0,"error":false,"latency_sec":316.46,"latency_breakdown":{"env_setup":30.936,"agent_setup":20.475,"agent_exec":244.864,"verifier":4.916},"taskName":"ashley_ecs_simulation_headless","jobId":"Z74yNfA","exec_duration":244.864},{"job_name":"2026-06-12__02-48-52","trial_name":"ashley_ecs_simulation_headless__vcCksmG","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":192.162,"latency_breakdown":{"env_setup":14.465,"agent_setup":20.355,"agent_exec":124.279,"verifier":14.136},"taskName":"ashley_ecs_simulation_headless","jobId":"vcCksmG","exec_duration":124.279},{"job_name":"2026-06-12__02-48-52","trial_name":"ashley_ecs_simulation_headless__CaFtZUQ","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":false,"reward":0,"error":false,"latency_sec":424.733,"latency_breakdown":{"env_setup":34.773,"agent_setup":21.808,"agent_exec":340.423,"verifier":14.28},"taskName":"ashley_ecs_simulation_headless","jobId":"CaFtZUQ","exec_duration":340.423}]},{"taskName":"deterministic_tick_simulation_headless","instruction":"$5","trials":[{"job_name":"2026-06-11__14-19-50","trial_name":"deterministic_tick_simulation_he__t5Dte7H","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":832.014,"latency_breakdown":{"env_setup":148.305,"agent_setup":31.867,"agent_exec":600.169,"verifier":32.803},"taskName":"deterministic_tick_simulation_he","jobId":"t5Dte7H","exec_duration":600.169},{"job_name":"2026-06-11__14-19-50","trial_name":"deterministic_tick_simulation_he__mCVWUTj","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":558.325,"latency_breakdown":{"env_setup":111.547,"agent_setup":50.576,"agent_exec":343.235,"verifier":36.322},"taskName":"deterministic_tick_simulation_he","jobId":"mCVWUTj","exec_duration":343.235},{"job_name":"2026-06-11__15-16-40","trial_name":"deterministic_tick_simulation_he__GmiVHVF","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":705.311,"latency_breakdown":{"env_setup":86.229,"agent_setup":38.195,"agent_exec":529.565,"verifier":33.912},"taskName":"deterministic_tick_simulation_he","jobId":"GmiVHVF","exec_duration":529.565},{"job_name":"2026-06-12__10-26-52","trial_name":"deterministic_tick_simulation_he__mJnVtvu","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":447.099,"latency_breakdown":{"env_setup":128.199,"agent_setup":23.912,"agent_exec":242.034,"verifier":39.592},"taskName":"deterministic_tick_simulation_he","jobId":"mJnVtvu","exec_duration":242.034},{"job_name":"2026-06-12__10-26-52","trial_name":"deterministic_tick_simulation_he__tRGSmKY","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":596.608,"latency_breakdown":{"env_setup":220.492,"agent_setup":23.571,"agent_exec":300.764,"verifier":34.071},"taskName":"deterministic_tick_simulation_he","jobId":"tRGSmKY","exec_duration":300.764},{"job_name":"2026-06-12__10-26-52","trial_name":"deterministic_tick_simulation_he__frYi5LK","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":422.878,"latency_breakdown":{"env_setup":181.851,"agent_setup":24.956,"agent_exec":157.158,"verifier":35.067},"taskName":"deterministic_tick_simulation_he","jobId":"frYi5LK","exec_duration":157.158}]},{"taskName":"i18n_bundle_localization_headless","instruction":"$6","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"i18n_bundle_localization_headles__G5uLk4a","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":389.534,"latency_breakdown":{"env_setup":97.091,"agent_setup":33.713,"agent_exec":233.072,"verifier":8.768},"taskName":"i18n_bundle_localization_headles","jobId":"G5uLk4a","exec_duration":233.072},{"job_name":"2026-06-12__09-28-23","trial_name":"i18n_bundle_localization_headles__WFrXVsi","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":421.681,"latency_breakdown":{"env_setup":163.031,"agent_setup":20.711,"agent_exec":201.664,"verifier":9.932},"taskName":"i18n_bundle_localization_headles","jobId":"WFrXVsi","exec_duration":201.664},{"job_name":"2026-06-12__09-28-23","trial_name":"i18n_bundle_localization_headles__PAGhsi7","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":666.335,"latency_breakdown":{"env_setup":47.023,"agent_setup":23.036,"agent_exec":571.715,"verifier":8.788},"taskName":"i18n_bundle_localization_headles","jobId":"PAGhsi7","exec_duration":571.715},{"job_name":"2026-06-12__02-48-52","trial_name":"i18n_bundle_localization_headles__wSzYi9j","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":782.171,"latency_breakdown":{"env_setup":12.265,"agent_setup":21.653,"agent_exec":600.205,"verifier":128.41},"taskName":"i18n_bundle_localization_headles","jobId":"wSzYi9j","exec_duration":600.205},{"job_name":"2026-06-12__02-48-52","trial_name":"i18n_bundle_localization_headles__4TnGabD","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":405.663,"latency_breakdown":{"env_setup":109.561,"agent_setup":22.112,"agent_exec":245.618,"verifier":9.197},"taskName":"i18n_bundle_localization_headles","jobId":"4TnGabD","exec_duration":245.618},{"job_name":"2026-06-12__02-48-52","trial_name":"i18n_bundle_localization_headles__r6Gmi66","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":336.096,"latency_breakdown":{"env_setup":139.63,"agent_setup":20.835,"agent_exec":151.01,"verifier":8.833},"taskName":"i18n_bundle_localization_headles","jobId":"r6Gmi66","exec_duration":151.01}]},{"taskName":"interpolation_curve_sampler_headless","instruction":"$7","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"interpolation_curve_sampler_head__acM9mNS","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":321.63,"latency_breakdown":{"env_setup":39.95,"agent_setup":20.801,"agent_exec":202.38,"verifier":44.364},"taskName":"interpolation_curve_sampler_head","jobId":"acM9mNS","exec_duration":202.38},{"job_name":"2026-06-12__09-28-23","trial_name":"interpolation_curve_sampler_head__cKfZM22","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":727.961,"latency_breakdown":{"env_setup":36.327,"agent_setup":21.873,"agent_exec":600.179,"verifier":52.665},"taskName":"interpolation_curve_sampler_head","jobId":"cKfZM22","exec_duration":600.179},{"job_name":"2026-06-12__09-28-23","trial_name":"interpolation_curve_sampler_head__YTHYeKr","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":423.506,"latency_breakdown":{"env_setup":27.749,"agent_setup":21.565,"agent_exec":315.798,"verifier":43.696},"taskName":"interpolation_curve_sampler_head","jobId":"YTHYeKr","exec_duration":315.798},{"job_name":"2026-06-12__02-48-52","trial_name":"interpolation_curve_sampler_head__Bzcfsuo","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":749.173,"latency_breakdown":{"env_setup":60.273,"agent_setup":20.386,"agent_exec":600.174,"verifier":43.602},"taskName":"interpolation_curve_sampler_head","jobId":"Bzcfsuo","exec_duration":600.174},{"job_name":"2026-06-12__02-48-52","trial_name":"interpolation_curve_sampler_head__AbD8tD5","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":398.368,"latency_breakdown":{"env_setup":68.293,"agent_setup":20.465,"agent_exec":250.425,"verifier":40.994},"taskName":"interpolation_curve_sampler_head","jobId":"AbD8tD5","exec_duration":250.425},{"job_name":"2026-06-12__02-48-52","trial_name":"interpolation_curve_sampler_head__YCxvqgQ","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":482.958,"latency_breakdown":{"env_setup":62.353,"agent_setup":21.763,"agent_exec":334.992,"verifier":46.694},"taskName":"interpolation_curve_sampler_head","jobId":"YCxvqgQ","exec_duration":334.992}]},{"taskName":"intersector_polygon_clip_headless","instruction":"$8","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"intersector_polygon_clip_headles__B8PjmSc","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":314.882,"latency_breakdown":{"env_setup":57.753,"agent_setup":20.81,"agent_exec":203.93,"verifier":13.577},"taskName":"intersector_polygon_clip_headles","jobId":"B8PjmSc","exec_duration":203.93},{"job_name":"2026-06-12__09-28-23","trial_name":"intersector_polygon_clip_headles__Ah5orYz","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":699.692,"latency_breakdown":{"env_setup":42.271,"agent_setup":21.454,"agent_exec":600.171,"verifier":17.767},"taskName":"intersector_polygon_clip_headles","jobId":"Ah5orYz","exec_duration":600.171},{"job_name":"2026-06-12__09-28-23","trial_name":"intersector_polygon_clip_headles__TqZZd5R","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":false,"reward":0,"error":false,"latency_sec":310.355,"latency_breakdown":{"env_setup":63.766,"agent_setup":19.688,"agent_exec":193.92,"verifier":13.288},"taskName":"intersector_polygon_clip_headles","jobId":"TqZZd5R","exec_duration":193.92},{"job_name":"2026-06-12__02-48-52","trial_name":"intersector_polygon_clip_headles__rsQF3vH","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":false,"reward":0,"error":false,"latency_sec":305.991,"latency_breakdown":{"env_setup":35.137,"agent_setup":21.06,"agent_exec":219.464,"verifier":14.765},"taskName":"intersector_polygon_clip_headles","jobId":"rsQF3vH","exec_duration":219.464},{"job_name":"2026-06-12__02-48-52","trial_name":"intersector_polygon_clip_headles__oDGbirc","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":716.347,"latency_breakdown":{"env_setup":65.58,"agent_setup":22.206,"agent_exec":600.184,"verifier":11.901},"taskName":"intersector_polygon_clip_headles","jobId":"oDGbirc","exec_duration":600.184},{"job_name":"2026-06-12__02-48-52","trial_name":"intersector_polygon_clip_headles__k3QNggv","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":280.146,"latency_breakdown":{"env_setup":80.762,"agent_setup":21.09,"agent_exec":147.814,"verifier":13.617},"taskName":"intersector_polygon_clip_headles","jobId":"k3QNggv","exec_duration":147.814}]},{"taskName":"pixmap_procedural_renderer_headless","instruction":"$9","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"pixmap_procedural_renderer_headl__gwR3zDv","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":785.219,"latency_breakdown":{"env_setup":192.231,"agent_setup":19.308,"agent_exec":495.091,"verifier":17.017},"taskName":"pixmap_procedural_renderer_headl","jobId":"gwR3zDv","exec_duration":495.091},{"job_name":"2026-06-12__09-28-23","trial_name":"pixmap_procedural_renderer_headl__QZ3Vm22","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":480.442,"latency_breakdown":{"env_setup":95.817,"agent_setup":35.089,"agent_exec":274.941,"verifier":20.516},"taskName":"pixmap_procedural_renderer_headl","jobId":"QZ3Vm22","exec_duration":274.941},{"job_name":"2026-06-12__09-28-23","trial_name":"pixmap_procedural_renderer_headl__PsZn3T6","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":581.044,"latency_breakdown":{"env_setup":164.138,"agent_setup":20.345,"agent_exec":328.558,"verifier":18.249},"taskName":"pixmap_procedural_renderer_headl","jobId":"PsZn3T6","exec_duration":328.558},{"job_name":"2026-06-12__02-48-52","trial_name":"pixmap_procedural_renderer_headl__3Q3rMyY","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":false,"reward":null,"error":true,"latency_sec":1133.762,"latency_breakdown":{"env_setup":152.403,"agent_setup":20.571,"agent_exec":334.257,"verifier":600.001},"taskName":"pixmap_procedural_renderer_headl","jobId":"3Q3rMyY","exec_duration":334.257},{"job_name":"2026-06-12__02-48-52","trial_name":"pixmap_procedural_renderer_headl__aQWcftJ","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":461.843,"latency_breakdown":{"env_setup":110.208,"agent_setup":22.704,"agent_exec":260.844,"verifier":19.117},"taskName":"pixmap_procedural_renderer_headl","jobId":"aQWcftJ","exec_duration":260.844},{"job_name":"2026-06-12__02-48-52","trial_name":"pixmap_procedural_renderer_headl__PJqQ8Bq","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":473.382,"latency_breakdown":{"env_setup":138.242,"agent_setup":20.561,"agent_exec":248.488,"verifier":19.338},"taskName":"pixmap_procedural_renderer_headl","jobId":"PJqQ8Bq","exec_duration":248.488}]},{"taskName":"pooled_projectile_simulation_headless","instruction":"$a","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"pooled_projectile_simulation_hea__nEy4cQF","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":375.896,"latency_breakdown":{"env_setup":21.766,"agent_setup":20.551,"agent_exec":278.044,"verifier":25.029},"taskName":"pooled_projectile_simulation_hea","jobId":"nEy4cQF","exec_duration":278.044},{"job_name":"2026-06-12__09-28-23","trial_name":"pooled_projectile_simulation_hea__DbpMcXG","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":335.728,"latency_breakdown":{"env_setup":31.798,"agent_setup":21.191,"agent_exec":234.431,"verifier":21.884},"taskName":"pooled_projectile_simulation_hea","jobId":"DbpMcXG","exec_duration":234.431},{"job_name":"2026-06-12__09-28-23","trial_name":"pooled_projectile_simulation_hea__2Ak8F4V","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":732.676,"latency_breakdown":{"env_setup":54.022,"agent_setup":22.174,"agent_exec":600.182,"verifier":23.444},"taskName":"pooled_projectile_simulation_hea","jobId":"2Ak8F4V","exec_duration":600.182},{"job_name":"2026-06-12__02-48-52","trial_name":"pooled_projectile_simulation_hea__Hbkzsvu","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":301.664,"latency_breakdown":{"env_setup":25.522,"agent_setup":20.492,"agent_exec":201.383,"verifier":22.021},"taskName":"pooled_projectile_simulation_hea","jobId":"Hbkzsvu","exec_duration":201.383},{"job_name":"2026-06-12__02-48-52","trial_name":"pooled_projectile_simulation_hea__F5vjedP","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":697.199,"latency_breakdown":{"env_setup":46.938,"agent_setup":21.203,"agent_exec":600.192,"verifier":0.768},"taskName":"pooled_projectile_simulation_hea","jobId":"F5vjedP","exec_duration":600.192},{"job_name":"2026-06-12__02-48-52","trial_name":"pooled_projectile_simulation_hea__2VMVbMN","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":481.715,"latency_breakdown":{"env_setup":11.757,"agent_setup":20.978,"agent_exec":403.985,"verifier":22.641},"taskName":"pooled_projectile_simulation_hea","jobId":"2VMVbMN","exec_duration":403.985}]},{"taskName":"scene2d_stage_action_replay_headless","instruction":"$b","trials":[{"job_name":"2026-06-11__14-19-50","trial_name":"scene2d_stage_action_replay_head__HZMtKzQ","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":837.778,"latency_breakdown":{"env_setup":150.205,"agent_setup":23.685,"agent_exec":600.192,"verifier":43.279},"taskName":"scene2d_stage_action_replay_head","jobId":"HZMtKzQ","exec_duration":600.192},{"job_name":"2026-06-11__14-19-50","trial_name":"scene2d_stage_action_replay_head__2oFyfnk","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":571.002,"latency_breakdown":{"env_setup":109.083,"agent_setup":52.972,"agent_exec":349.55,"verifier":42.73},"taskName":"scene2d_stage_action_replay_head","jobId":"2oFyfnk","exec_duration":349.55},{"job_name":"2026-06-11__15-16-40","trial_name":"scene2d_stage_action_replay_head__k8CGBVc","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":591.458,"latency_breakdown":{"env_setup":66.387,"agent_setup":40.749,"agent_exec":425.795,"verifier":42.814},"taskName":"scene2d_stage_action_replay_head","jobId":"k8CGBVc","exec_duration":425.795},{"job_name":"2026-06-12__10-26-52","trial_name":"scene2d_stage_action_replay_head__siejsDC","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":919.126,"latency_breakdown":{"env_setup":208.264,"agent_setup":20.973,"agent_exec":600.178,"verifier":73.768},"taskName":"scene2d_stage_action_replay_head","jobId":"siejsDC","exec_duration":600.178},{"job_name":"2026-06-12__10-26-52","trial_name":"scene2d_stage_action_replay_head__58e9yuu","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":545.487,"latency_breakdown":{"env_setup":128.833,"agent_setup":28.071,"agent_exec":327.956,"verifier":44.418},"taskName":"scene2d_stage_action_replay_head","jobId":"58e9yuu","exec_duration":327.956},{"job_name":"2026-06-12__10-26-52","trial_name":"scene2d_stage_action_replay_head__hGuX6MT","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":479.483,"latency_breakdown":{"env_setup":185.072,"agent_setup":26.09,"agent_exec":213.066,"verifier":40.526},"taskName":"scene2d_stage_action_replay_head","jobId":"hGuX6MT","exec_duration":213.066}]},{"taskName":"scripted_input_replay_headless","instruction":"$c","trials":[{"job_name":"2026-06-11__14-19-50","trial_name":"scripted_input_replay_headless__FFJ5cSK","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":460.211,"latency_breakdown":{"env_setup":111.722,"agent_setup":50.284,"agent_exec":275.224,"verifier":8.677},"taskName":"scripted_input_replay_headless","jobId":"FFJ5cSK","exec_duration":275.224},{"job_name":"2026-06-11__14-19-50","trial_name":"scripted_input_replay_headless__UDNGDk6","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":778.425,"latency_breakdown":{"env_setup":149.274,"agent_setup":24.933,"agent_exec":576.091,"verifier":9.733},"taskName":"scripted_input_replay_headless","jobId":"UDNGDk6","exec_duration":576.091},{"job_name":"2026-06-11__15-16-40","trial_name":"scripted_input_replay_headless__kxZiu7N","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":619.4,"latency_breakdown":{"env_setup":85.503,"agent_setup":49.632,"agent_exec":459.46,"verifier":8.404},"taskName":"scripted_input_replay_headless","jobId":"kxZiu7N","exec_duration":459.46},{"job_name":"2026-06-12__10-26-52","trial_name":"scripted_input_replay_headless__Rgw55x6","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":383.158,"latency_breakdown":{"env_setup":185.191,"agent_setup":29.741,"agent_exec":142.87,"verifier":8.196},"taskName":"scripted_input_replay_headless","jobId":"Rgw55x6","exec_duration":142.87},{"job_name":"2026-06-12__10-26-52","trial_name":"scripted_input_replay_headless__XNBRWLS","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":649.243,"latency_breakdown":{"env_setup":128.286,"agent_setup":29.214,"agent_exec":464.282,"verifier":8.92},"taskName":"scripted_input_replay_headless","jobId":"XNBRWLS","exec_duration":464.282},{"job_name":"2026-06-12__10-26-52","trial_name":"scripted_input_replay_headless__JVKZbBK","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":699.757,"latency_breakdown":{"env_setup":50.09,"agent_setup":22.85,"agent_exec":600.2,"verifier":8.893},"taskName":"scripted_input_replay_headless","jobId":"JVKZbBK","exec_duration":600.2}]},{"taskName":"shape_overlap_collision_report_headless","instruction":"$d","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"shape_overlap_collision_report_h__Emisa2V","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":394.145,"latency_breakdown":{"env_setup":51.126,"agent_setup":22.482,"agent_exec":290.304,"verifier":8.971},"taskName":"shape_overlap_collision_report_h","jobId":"Emisa2V","exec_duration":290.304},{"job_name":"2026-06-12__09-28-23","trial_name":"shape_overlap_collision_report_h__EzygHqQ","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":668.341,"latency_breakdown":{"env_setup":17.834,"agent_setup":25.614,"agent_exec":600.171,"verifier":8.289},"taskName":"shape_overlap_collision_report_h","jobId":"EzygHqQ","exec_duration":600.171},{"job_name":"2026-06-12__09-28-23","trial_name":"shape_overlap_collision_report_h__u8QDfDx","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":281.811,"latency_breakdown":{"env_setup":40.204,"agent_setup":20.271,"agent_exec":187.931,"verifier":9.869},"taskName":"shape_overlap_collision_report_h","jobId":"u8QDfDx","exec_duration":187.931},{"job_name":"2026-06-12__02-48-52","trial_name":"shape_overlap_collision_report_h__bKWxpTK","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":677.888,"latency_breakdown":{"env_setup":31.285,"agent_setup":20.234,"agent_exec":600.244,"verifier":0.807},"taskName":"shape_overlap_collision_report_h","jobId":"bKWxpTK","exec_duration":600.244},{"job_name":"2026-06-12__02-48-52","trial_name":"shape_overlap_collision_report_h__YCk8TaV","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":244.93,"latency_breakdown":{"env_setup":24.027,"agent_setup":21.882,"agent_exec":175.021,"verifier":8.565},"taskName":"shape_overlap_collision_report_h","jobId":"YCk8TaV","exec_duration":175.021},{"job_name":"2026-06-12__02-48-52","trial_name":"shape_overlap_collision_report_h__BVqNWTz","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":354.938,"latency_breakdown":{"env_setup":10.91,"agent_setup":19.93,"agent_exec":300.337,"verifier":9.321},"taskName":"shape_overlap_collision_report_h","jobId":"BVqNWTz","exec_duration":300.337}]},{"taskName":"turn_based_command_loop_headless","instruction":"$e","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"turn_based_command_loop_headless__QvquU77","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":true,"latency_sec":729.361,"latency_breakdown":{"env_setup":40.71,"agent_setup":23.807,"agent_exec":600.19,"verifier":44.591},"taskName":"turn_based_command_loop_headless","jobId":"QvquU77","exec_duration":600.19},{"job_name":"2026-06-12__09-28-23","trial_name":"turn_based_command_loop_headless__r6brzHT","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":352.671,"latency_breakdown":{"env_setup":13.155,"agent_setup":21.028,"agent_exec":264.721,"verifier":36.769},"taskName":"turn_based_command_loop_headless","jobId":"r6brzHT","exec_duration":264.721},{"job_name":"2026-06-12__09-28-23","trial_name":"turn_based_command_loop_headless__rZGo9T4","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":391.792,"latency_breakdown":{"env_setup":53.244,"agent_setup":20.087,"agent_exec":267.911,"verifier":36.361},"taskName":"turn_based_command_loop_headless","jobId":"rZGo9T4","exec_duration":267.911},{"job_name":"2026-06-12__02-48-52","trial_name":"turn_based_command_loop_headless__d69dCjS","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":411.664,"latency_breakdown":{"env_setup":56.672,"agent_setup":20.54,"agent_exec":283.862,"verifier":36.63},"taskName":"turn_based_command_loop_headless","jobId":"d69dCjS","exec_duration":283.862},{"job_name":"2026-06-12__02-48-52","trial_name":"turn_based_command_loop_headless__33fZhH4","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":false,"reward":0,"error":false,"latency_sec":427.581,"latency_breakdown":{"env_setup":20.487,"agent_setup":20.575,"agent_exec":369.865,"verifier":0.747},"taskName":"turn_based_command_loop_headless","jobId":"33fZhH4","exec_duration":369.865},{"job_name":"2026-06-12__02-48-52","trial_name":"turn_based_command_loop_headless__aA7QQdx","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":false,"reward":0,"error":true,"latency_sec":696.232,"latency_breakdown":{"env_setup":59.502,"agent_setup":19.675,"agent_exec":600.17,"verifier":0.827},"taskName":"turn_based_command_loop_headless","jobId":"aA7QQdx","exec_duration":600.17}]},{"taskName":"xml_reader_scene_graph_headless","instruction":"$f","trials":[{"job_name":"2026-06-12__09-28-23","trial_name":"xml_reader_scene_graph_headless__gM3CsnH","model":"deepseek-4-pro","rawModel":"deepseek/deepseek-4-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":612.018,"latency_breakdown":{"env_setup":29.09,"agent_setup":24.668,"agent_exec":507.403,"verifier":36.609},"taskName":"xml_reader_scene_graph_headless","jobId":"gM3CsnH","exec_duration":507.403},{"job_name":"2026-06-12__09-28-23","trial_name":"xml_reader_scene_graph_headless__D2ZWBKX","model":"gemini-3.5-flash","rawModel":"google/gemini-3.5-flash","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":381.134,"latency_breakdown":{"env_setup":69.567,"agent_setup":36.25,"agent_exec":218.765,"verifier":36.629},"taskName":"xml_reader_scene_graph_headless","jobId":"D2ZWBKX","exec_duration":218.765},{"job_name":"2026-06-12__09-28-23","trial_name":"xml_reader_scene_graph_headless__ErYh9ZP","model":"gpt-5.5","rawModel":"openai/gpt-5.5","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":334.043,"latency_breakdown":{"env_setup":69.292,"agent_setup":29.644,"agent_exec":175.311,"verifier":38.905},"taskName":"xml_reader_scene_graph_headless","jobId":"ErYh9ZP","exec_duration":175.311},{"job_name":"2026-06-12__02-48-52","trial_name":"xml_reader_scene_graph_headless__ZmC7RNY","model":"gemini-3.1-pro","rawModel":"google/gemini-3.1-pro","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":387.473,"latency_breakdown":{"env_setup":76.583,"agent_setup":19.887,"agent_exec":234.531,"verifier":36.268},"taskName":"xml_reader_scene_graph_headless","jobId":"ZmC7RNY","exec_duration":234.531},{"job_name":"2026-06-12__02-48-52","trial_name":"xml_reader_scene_graph_headless__B2LcCAx","model":"claude-4-6-sonnet","rawModel":"anthropic/claude-4-6-sonnet","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":349.882,"latency_breakdown":{"env_setup":59.179,"agent_setup":21.072,"agent_exec":214.558,"verifier":36.516},"taskName":"xml_reader_scene_graph_headless","jobId":"B2LcCAx","exec_duration":214.558},{"job_name":"2026-06-12__02-48-52","trial_name":"xml_reader_scene_graph_headless__5WJ54KZ","model":"glm-5.1","rawModel":"zai/glm-5.1","agent":"Pochi","passed":true,"reward":1,"error":false,"latency_sec":693.351,"latency_breakdown":{"env_setup":37.787,"agent_setup":20.949,"agent_exec":582.945,"verifier":35.1},"taskName":"xml_reader_scene_graph_headless","jobId":"5WJ54KZ","exec_duration":582.945}]}]}]}]]}],["$L10","$L11"],"$L12"]}],"loading":null,"isPartial":false}
10:["$","script","script-0",{"src":"/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/c63472aac096477d.js","async":true}]
11:["$","script","script-1",{"src":"/libgdx-benchmark/pr-preview/pr-3/_next/static/chunks/f9f579c42c486944.js","async":true}]
12:["$","$L13",null,{"children":["$","$2",null,{"name":"Next.MetadataOutlet","children":"$@14"}]}]
14:null
